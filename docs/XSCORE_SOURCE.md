# XScore source

`profiles.xscore` and `orgs.xscore` are stored values in the database. The Wallchain API is not integrated in this repo today. Values may be set manually or via an external worker/process. On the public 1-pager, XScore is displayed with a small label: "Stored value (manual until Wallchain sync)" so viewers do not assume live syncing.
