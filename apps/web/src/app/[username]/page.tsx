"use client";

/**
 * Public profile URL: /[username] (e.g. /muazxinthix).
 * Renders the main app; route is derived from pathname and shows UserProfilePage for that username.
 */
import AppWithProviders from "../AppWithProviders";

export default function UsernamePage() {
  return <AppWithProviders />;
}
